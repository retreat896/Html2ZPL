{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "name": "Dev",
            "program": "${workspaceFolder}/start-dev.js",
            "console": "integratedTerminal",
            "cwd": "${workspaceFolder}"
        }
    ]
}
